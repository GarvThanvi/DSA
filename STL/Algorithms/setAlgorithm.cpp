#include<iostream>
#include<vector>
#include<algorithm>
using namespace std;

int main(){
    // set_union(first.begin(), first.end(), second.begin(), second.end(), inserter(newvector, newvector.begin()));

    //set_intersection same parameters as set_union
    //set_difference
    //set_symmetric_difference // gives symmetric difference of two sorted vectors
    return 0;
}