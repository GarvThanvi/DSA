#include<iostream>
#include<vector>
#include<algorithm>
using namespace std;

int main(){
    //all vectors need to be sorted for all above algorithms
    //bool a = binary_search(arr.begin(), arr.end(), target);

    //auto it = lower_bound(arr.begin(), arr.end(), 35); //returns first value >= 35
    //auto it = upper_bound(arr.begin(), arr.end(), 35); //returns first value > 35
    //p = equal_range(arr.begin(), arr.end(), 10); //return a pair of iterators that give us the ranges [first,last) from which 10 exists



    return 0;
}